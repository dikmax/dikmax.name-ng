/* TODO @utility */
.hidden {
    display: none;
}

.dark-background {
    color: var(--dark-background-text);

    & a {
        color: var(--dark-background-text);
    }
}

.light-background {
    color: var(--light-background-text);

    & a {
        color: var(--light-background-text);
    }
}
